# SymbolsGenerator

This command line tool creates most of the `SFSymbols` code by converting the input files to Swift code.

The tool is best used by opening a terminal, changing to the main `SFSymbols` directory and running `make generate-symbol`. The relevant files are then (over)written automatically. Newly created files must be added manually to the `SFSymbols` Xcode project.
